# 更新公告

## v0.2.0 — 2026-09-15

> 本次更新聚焦稳定性、稳健性与使用体验，修复 6 项关键问题，新增多项体验功能。

### 🐛 问题修复

- **B站请求超时**：所有 API 请求统一增加 15 秒超时，杜绝网络挂起导致界面永久卡在"分析中"
- **界面卡死修复**：传统模式采集异常时按钮不再永久禁用，任何路径都会复位状态
- **评论翻页矛盾**：评论采集工具补上 `page` 参数，提示词"建议获取 2-3 页"与工具能力不再脱节
- **WBI 签名失效**：密钥缓存增加 12 小时过期判断，签名失败自动刷新重试
- **配置丢失**：配置文件迁移到 `~/.sonar/config.json`，修复 PyInstaller 打包后每次退出配置丢失；旧配置自动迁移
- **跨线程隐患**：修复工作线程直接访问界面控件的隐患，采集参数改由主线程预先读取

### 🛡️ 稳定性增强

- **请求限流**：B站 / 贴吧相邻请求增加最小间隔，降低触发风控概率
- **风控退避**：遇到 -412/-509 等风控返回码时指数退避重试；被风控拦截与"评论区关闭"明确区分
- **安全验证检测**：贴吧返回安全验证页时给出明确提示，不再把反爬页误报为"没有讨论"
- **上下文保护**：工具结果按长度截断后写入对话，防止多轮采集把上下文撑爆
- **Prompt 注入缓解**：声明评论等外部内容仅为分析素材，降低指令注入干扰
- **评论热度排序**：舆情分析默认按热度取评论，结论更具代表性

### ✨ 新功能

- **报告流式输出**：LLM 生成报告时逐字实时显示，告别长时间空屏等待；服务端不支持流式时自动降级
- **Markdown 渲染预览**：标题、表格、引用、代码块等一键渲染，支持渲染 / 原文切换与一键复制
- **分析历史归档**：每次分析自动保存报告 + 原始数据到 `~/.sonar/history/`，可随时回看
- **LLM 用量统计**：每次分析显示 token 消耗与请求次数

### 📦 使用提醒

- 本版起配置文件位置从脚本目录迁移至 `~/.sonar/config.json`，旧配置会自动迁移，无需手动操作
